# High-Availability Application on AWS

High-availability simply means the application still works even when the server is down. To achieve high-availability the application is deployed on multiple servers.
